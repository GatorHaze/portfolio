{
  "private": true,
  "scripts": {
    "deploy": "surge ./public",
    "start": "browser-sync start --server 'public' --files 'public'"
  },
  "devDependencies": {
    "browser-sync": "^2.18.2",
    "stylelint": "^7.6.0",
    "stylelint-config-standard": "^15.0.0",
    "surge": "^0.18.0"
  }
}
